/// Barrel: [DsStatusPill] — see `../ds/status_pill.dart` for per-field token docs.
/// Summary: [TokenReference] `surface.*`, `text.*`, `spacing.space*`,
/// `radius.radius1000`, `typography.body.*`.
library;

export '../ds/status_pill.dart';
