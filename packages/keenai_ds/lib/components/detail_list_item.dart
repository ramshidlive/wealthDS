/// Barrel: [DsDetailListItem] — see `../ds/detail_list_item.dart` for token docs.
/// Summary: [TokenReference] `surface.card`, `border.light`, `radius.radius8`,
/// `text.*`, `spacing.*`, `typography.body.*`.
library;

export '../ds/detail_list_item.dart';
