/// Barrel: [DsStatBox] — token usage is documented on implementation fields in
/// `../ds/stat_box.dart`. Summary: [TokenReference] keys `typography.body.*`,
/// `spacing.space*`, `text.main`, `text.muted`.
library;

export '../ds/stat_box.dart';
