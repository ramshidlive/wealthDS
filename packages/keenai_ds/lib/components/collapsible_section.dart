/// Barrel: [DsCollapsibleSection] — see `../ds/collapsible_section.dart` for token docs.
/// Summary: [TokenReference] `surface.white`, `border.medium`, `radius.radius12`,
/// `spacing.*`, `text.*`, `typography.body.*`.
library;

export '../ds/collapsible_section.dart';
