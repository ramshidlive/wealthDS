/// Barrel: [TileLeading] — atom-level leading block for list rows.
/// Source lives in `../src/atoms/tile_leading.dart`.
/// Summary: [TokenReference] `text.*`, `surface.*`, `border.*`, `banner.success*`,
/// `spacing.*`, `radius.*`, `typography.body.*`.
library;

export '../src/atoms/tile_leading.dart';
