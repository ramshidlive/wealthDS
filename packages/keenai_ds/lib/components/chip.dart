/// Barrel: [DsChip] — see `../ds/chip.dart` for per-field token docs.
/// Summary: [TokenReference] `text.*`, `surface.*`, `border.*`, `spacing.*`,
/// `radius.radius1000`, `typography.body.*`.
library;

export '../ds/chip.dart';
